import '../commons.dart';

class SurahAudioScreen extends StatelessWidget {
  const SurahAudioScreen({super.key, required String title, required String url});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
