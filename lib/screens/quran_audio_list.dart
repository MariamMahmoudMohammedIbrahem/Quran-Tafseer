import '../commons.dart';

class QuranAudioListScreen extends StatelessWidget {
  const QuranAudioListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
